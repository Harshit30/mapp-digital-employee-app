import React from "react";
import classes from "./Header.module.css";

const Header = () => (
    <div className={classes.Header}>
      <p className={classes.HeaderText}>The Employee App</p>
    </div>
);

export default Header;
